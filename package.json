{
  "name": "sanal-pc",
  "version": "1.0.0",
  "description": "CV ve Blog Sitesi",
  "scripts": {
    "start": "cd client && npm start",
    "server": "cd server && npm run dev",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run server\" \"npm run start\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "react-quill": "^2.0.0",
    "remark-gfm": "^4.0.1"
  }
}
